DB_HOST=localhost           # database host
DB_PORT=5666                # database port
DB_USER=postgres            # database user
DB_PASS=mota                # database password
DB_NAME=postgres            # database name
PORT=3000                   # application port
environment=dev           # running environment
JWT_SECRET=serendipity  #value used to seed the JWT
RSA_PASSPHRASE=fooBarxyz
FRONT_URL=localhost:5173    #url of local host to add to cors
CONTENT_SERVER_URL=http://localhost:3001   #content server url